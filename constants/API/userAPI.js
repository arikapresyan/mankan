import keys from '../keys';

const paths = {
  login: `${keys.HOST}/login`,
  registration: `${keys.HOST}/registration`,
  recoveringPassword: `${keys.HOST}/recoveringPassword`,
};
export default paths;
